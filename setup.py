from setuptools import setup, find_packages

setup(
    name="py-logger-utils",
    version="1.0.1",
    description="Helper utilities for logging and debugging",
    author="Open Source Community",
    packages=find_packages(),
    install_requires=["requests"]
)
